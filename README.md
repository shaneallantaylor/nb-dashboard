# Dashboard Mockup for NationBuilder

To view, run the following commands in your terminal:
* `npm install`
* `npm run build`
* `npm start`
* Open your favorite browser and point all headings to `localhost:3000`